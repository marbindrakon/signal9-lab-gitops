apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: matrix-import-token
  namespace: home-infra
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-wheatley
  target:
    name: matrix-import-token
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: matrix-import-token
