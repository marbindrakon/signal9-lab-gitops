apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: px-central-helm
  namespace: openshift-gitops
spec:
  destination:
    namespace: px-central
    server: "https://kubernetes.default.svc"
  project: default
  syncPolicy:
    automated: {}
  source:
    repoURL: "https://charts.portworx.io"
    chart: "px-central"
    targetRevision: 2.5.1
    helm:
      releaseName: px-central
      values: |
        pxlicenseserver:
          enabled: true
        persistentStorage:
          enabled: true
          storageClassName: ocs-external-storagecluster-ceph-rbd
